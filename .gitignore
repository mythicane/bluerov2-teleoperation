__pycache__/
*.pyc
.claude/
.vscode/
wandb/

# trained checkpoints / extracted archives
models/
*.zip

# DAgger data collected by deployment_scripts/deploy_bluerov.py
dagger_data/

# Recorded episode data: keep the empty grab-*-rod folder skeletons (and
# their .gitkeep files) checked in, but never commit actual recordings.
data/grab */data/chunk-*/*.parquet
data/grab */videos/**/*.mp4
data/grab */meta/*.json
data/grab */meta/*.parquet
data/grab */meta/episodes/
